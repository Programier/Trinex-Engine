set(PLATFORM_ANDROID_SOURCES_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
file(GLOB PLATFORM_ANDROID_SRC "${PLATFORM_ANDROID_SOURCES_DIR}/*.cpp")


add_trinex_sources(${PLATFORM_ANDROID_SRC})
add_trinex_private_libraries(dl)
