
if(WIN32 OR LINUX)
    set(PLATFORM_COMMON_SOURCES_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
    file(GLOB PLATFORM_COMMON_SRC "${PLATFORM_COMMON_SOURCES_DIR}/*.cpp")

    set(TRINEX_ENGINE_PRIVATE_INCLUDE_DIRS ${TRINEX_ENGINE_PRIVATE_INCLUDE_DIRS} "${CMAKE_CURRENT_SOURCE_DIR}/../include/" PARENT_SCOPE)
    set(TRINEX_ENGINE_SOURCE_FILES ${TRINEX_ENGINE_SOURCE_FILES} ${PLATFORM_COMMON_SRC} PARENT_SCOPE)
    set(TRINEX_ENGINE_PRIVATE_LIBRARIES ${TRINEX_ENGINE_PRIVATE_LIBRARIES} SDL2-static PARENT_SCOPE)
endif()
